---
phase: 06-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/gsd/help.md
  - README.md
  - CHANGELOG.md
autonomous: true

must_haves:
  truths:
    - "User reading help.md sees unlimited listed with quality/balanced/budget"
    - "User reading README.md can discover unlimited profile and GSD_DEFAULT_MODEL_PROFILE env var"
    - "CHANGELOG.md documents both new features under [Unreleased]"
  artifacts:
    - path: "commands/gsd/help.md"
      provides: "Updated profile list with unlimited"
      contains: "`unlimited` — Opus for all agents"
    - path: "README.md"
      provides: "Updated Model Profiles table and new Environment Variables section"
      contains: "| `unlimited` | Opus | Opus | Opus |"
    - path: "CHANGELOG.md"
      provides: "New feature entries"
      contains: "Unlimited model profile"
  key_links:
    - from: "help.md profile list"
      to: "model-profiles.md table"
      via: "consistent profile naming"
      pattern: "unlimited.*Opus for all"
    - from: "README.md table"
      to: "model-profiles.md table"
      via: "matching profile descriptions"
      pattern: "unlimited.*Opus.*Opus.*Opus"
---

<objective>
Update user-facing documentation to document the unlimited model profile and GSD_DEFAULT_MODEL_PROFILE environment variable.

Purpose: Users can discover and understand the new features through documentation (DOC-02, DOC-03, DOC-04). DOC-01 was completed in Phase 1.
Output: Updated help.md, README.md, and CHANGELOG.md with unlimited profile and env var documentation.
</objective>

<execution_context>
@/Users/vmakaev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vmakaev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-documentation/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add unlimited to help.md profile list (DOC-02)</name>
  <files>commands/gsd/help.md</files>
  <action>
Update the `/gsd:set-profile` section (around line 316-322) to add unlimited as the first profile option.

Current:
```markdown
**`/gsd:set-profile <profile>`**
Quick switch model profile for GSD agents.

- `quality` — Opus everywhere except verification
- `balanced` — Opus for planning, Sonnet for execution (default)
- `budget` — Sonnet for writing, Haiku for research/verification
```

Update to:
```markdown
**`/gsd:set-profile <profile>`**
Quick switch model profile for GSD agents.

- `unlimited` — Opus for all agents (maximum quality)
- `quality` — Opus everywhere except verification
- `balanced` — Opus for planning, Sonnet for execution (default)
- `budget` — Sonnet for writing, Haiku for research/verification
```

Also update the `/gsd:settings` description (around line 311) to list unlimited in the profile options:
```markdown
- Select model profile (unlimited/quality/balanced/budget)
```
  </action>
  <verify>grep -n "unlimited" commands/gsd/help.md | head -5</verify>
  <done>help.md lists unlimited as first profile option in set-profile section with description "Opus for all agents (maximum quality)"</done>
</task>

<task type="auto">
  <name>Task 2: Update README.md with unlimited profile and env var (DOC-03)</name>
  <files>README.md</files>
  <action>
Two changes to README.md:

1. **Update Model Profiles table** (around line 495):
   Add unlimited as first data row.

   Current:
   ```markdown
   | Profile | Planning | Execution | Verification |
   |---------|----------|-----------|--------------|
   | `quality` | Opus | Opus | Sonnet |
   | `balanced` (default) | Opus | Sonnet | Sonnet |
   | `budget` | Sonnet | Sonnet | Haiku |
   ```

   Update to:
   ```markdown
   | Profile | Planning | Execution | Verification |
   |---------|----------|-----------|--------------|
   | `unlimited` | Opus | Opus | Opus |
   | `quality` | Opus | Opus | Sonnet |
   | `balanced` (default) | Opus | Sonnet | Sonnet |
   | `budget` | Sonnet | Sonnet | Haiku |
   ```

2. **Update the set-profile command in Utilities table** (around line 470):
   Change "quality/balanced/budget" to "unlimited/quality/balanced/budget".

   Current:
   ```markdown
   | `/gsd:set-profile <profile>` | Switch model profile (quality/balanced/budget) |
   ```

   Update to:
   ```markdown
   | `/gsd:set-profile <profile>` | Switch model profile (unlimited/quality/balanced/budget) |
   ```

3. **Add Environment Variables section** after the Workflow Agents section (after line 521, before Execution section):
   Insert new subsection:

   ```markdown
   ### Environment Variables

   | Variable | Default | What it does |
   |----------|---------|--------------|
   | `GSD_DEFAULT_MODEL_PROFILE` | `balanced` | Default model profile when no `.planning/config.json` exists |

   Fallback chain: `config.json` -> `GSD_DEFAULT_MODEL_PROFILE` -> `balanced`

   ```
  </action>
  <verify>grep -n "unlimited\|GSD_DEFAULT_MODEL_PROFILE" README.md | head -10</verify>
  <done>README.md has unlimited row in Model Profiles table and new Environment Variables section documenting GSD_DEFAULT_MODEL_PROFILE</done>
</task>

<task type="auto">
  <name>Task 3: Add CHANGELOG.md entry for new features (DOC-04)</name>
  <files>CHANGELOG.md</files>
  <action>
Add new feature entries under the [Unreleased] section (after line 7).

Current:
```markdown
## [Unreleased]

## [1.11.1] - 2026-01-31
```

Update to:
```markdown
## [Unreleased]

### Added
- **Unlimited model profile** — Use Opus for all agents without exception (`/gsd:set-profile unlimited` or configure in `/gsd:settings`)
- **GSD_DEFAULT_MODEL_PROFILE** environment variable — Set default profile when no config.json exists (fallback: config.json -> env var -> "balanced")

## [1.11.1] - 2026-01-31
```

Follow Keep a Changelog format:
- Use `### Added` for new features
- Bold the feature name
- Describe what it does in the em-dash description
  </action>
  <verify>grep -A5 "\[Unreleased\]" CHANGELOG.md</verify>
  <done>CHANGELOG.md has ### Added section under [Unreleased] with entries for unlimited profile and GSD_DEFAULT_MODEL_PROFILE env var</done>
</task>

</tasks>

<verification>
After all tasks complete, verify:
1. `grep "unlimited" commands/gsd/help.md` shows profile listing
2. `grep "unlimited\|GSD_DEFAULT_MODEL_PROFILE" README.md` shows table row and env var section
3. `grep -A5 "\[Unreleased\]" CHANGELOG.md` shows new entries
4. No duplicate entries or broken markdown formatting
</verification>

<success_criteria>
- DOC-02: help.md lists unlimited as first profile option with proper description
- DOC-03: README.md has unlimited row in Model Profiles table AND Environment Variables section with GSD_DEFAULT_MODEL_PROFILE
- DOC-04: CHANGELOG.md has ### Added section under [Unreleased] with both new features documented
- All changes follow established markdown patterns (backticks for code, em-dash descriptions, table formatting)
</success_criteria>

<output>
After completion, create `.planning/phases/06-documentation/06-01-SUMMARY.md`
</output>
