---
phase: 02-command-tables
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/gsd/audit-milestone.md
  - commands/gsd/debug.md
  - commands/gsd/execute-phase.md
  - commands/gsd/new-milestone.md
  - commands/gsd/new-project.md
  - commands/gsd/plan-phase.md
  - commands/gsd/quick.md
  - commands/gsd/research-phase.md
autonomous: true

must_haves:
  truths:
    - "All 8 command files have unlimited column in their model lookup tables"
    - "Unlimited column appears first after Agent column (before quality)"
    - "All unlimited column values are opus"
    - "Existing quality/balanced/budget values unchanged"
  artifacts:
    - path: "commands/gsd/audit-milestone.md"
      provides: "Model lookup with unlimited column"
      contains: "| Agent | `unlimited` | `quality` | `balanced` | `budget` |"
    - path: "commands/gsd/debug.md"
      provides: "Model lookup with unlimited column"
      contains: "| Agent | `unlimited` | `quality` | `balanced` | `budget` |"
    - path: "commands/gsd/execute-phase.md"
      provides: "Model lookup with unlimited column"
      contains: "| Agent | `unlimited` | `quality` | `balanced` | `budget` |"
    - path: "commands/gsd/new-milestone.md"
      provides: "Model lookup with unlimited column"
      contains: "| Agent | `unlimited` | `quality` | `balanced` | `budget` |"
    - path: "commands/gsd/new-project.md"
      provides: "Model lookup with unlimited column"
      contains: "| Agent | `unlimited` | `quality` | `balanced` | `budget` |"
    - path: "commands/gsd/plan-phase.md"
      provides: "Model lookup with unlimited column"
      contains: "| Agent | `unlimited` | `quality` | `balanced` | `budget` |"
    - path: "commands/gsd/quick.md"
      provides: "Model lookup with unlimited column"
      contains: "| Agent | `unlimited` | `quality` | `balanced` | `budget` |"
    - path: "commands/gsd/research-phase.md"
      provides: "Model lookup with unlimited column"
      contains: "| Agent | `unlimited` | `quality` | `balanced` | `budget` |"
  key_links:
    - from: "commands/gsd/*.md lookup tables"
      to: "get-shit-done/references/model-profiles.md"
      via: "column order consistency"
      pattern: "unlimited.*quality.*balanced.*budget"
---

<objective>
Add unlimited column to model lookup tables in all 8 GSD command files

Purpose: Enable unlimited profile selection in command orchestrators, completing the second tier of the profile distribution hierarchy (master -> commands -> workflows)
Output: All 8 command files have 4-column lookup tables matching master reference format
</objective>

<execution_context>
@/Users/vmakaev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vmakaev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-command-tables/02-RESEARCH.md
@get-shit-done/references/model-profiles.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add unlimited column to first 4 command files</name>
  <files>
    commands/gsd/audit-milestone.md
    commands/gsd/debug.md
    commands/gsd/execute-phase.md
    commands/gsd/new-milestone.md
  </files>
  <action>
For each file, update the model lookup table:

**audit-milestone.md (line 54):**
Current:
```markdown
| Agent | quality | balanced | budget |
|-------|---------|----------|--------|
| gsd-integration-checker | sonnet | sonnet | haiku |
```
Target:
```markdown
| Agent | `unlimited` | `quality` | `balanced` | `budget` |
|-------|-------------|-----------|------------|----------|
| gsd-integration-checker | opus | sonnet | sonnet | haiku |
```

**debug.md (line 43):**
Current:
```markdown
| Agent | quality | balanced | budget |
|-------|---------|----------|--------|
| gsd-debugger | opus | sonnet | sonnet |
```
Target:
```markdown
| Agent | `unlimited` | `quality` | `balanced` | `budget` |
|-------|-------------|-----------|------------|----------|
| gsd-debugger | opus | opus | sonnet | sonnet |
```

**execute-phase.md (line 52 - note: indented with 3 spaces):**
Current:
```markdown
   | Agent | quality | balanced | budget |
   |-------|---------|----------|--------|
   | gsd-executor | opus | sonnet | sonnet |
   | gsd-verifier | sonnet | sonnet | haiku |
```
Target:
```markdown
   | Agent | `unlimited` | `quality` | `balanced` | `budget` |
   |-------|-------------|-----------|------------|----------|
   | gsd-executor | opus | opus | sonnet | sonnet |
   | gsd-verifier | opus | sonnet | sonnet | haiku |
```

**new-milestone.md (line 137):**
Current:
```markdown
| Agent | quality | balanced | budget |
|-------|---------|----------|--------|
| gsd-project-researcher | opus | sonnet | haiku |
| gsd-research-synthesizer | sonnet | sonnet | haiku |
| gsd-roadmapper | opus | sonnet | sonnet |
```
Target:
```markdown
| Agent | `unlimited` | `quality` | `balanced` | `budget` |
|-------|-------------|-----------|------------|----------|
| gsd-project-researcher | opus | opus | sonnet | haiku |
| gsd-research-synthesizer | opus | sonnet | sonnet | haiku |
| gsd-roadmapper | opus | opus | sonnet | sonnet |
```

**Critical format requirements:**
- Use backticks around profile names in header: `unlimited`, `quality`, `balanced`, `budget`
- Add separator column: `-------------|`
- Column order: unlimited first after Agent
- Preserve existing indentation (execute-phase.md has 3-space indent)
- Do NOT modify any other part of the files
  </action>
  <verify>
Run: `grep -A5 "| Agent |" commands/gsd/audit-milestone.md commands/gsd/debug.md commands/gsd/execute-phase.md commands/gsd/new-milestone.md`
Confirm each table has 5 columns with unlimited first after Agent.
Confirm all unlimited values are opus.
  </verify>
  <done>
- audit-milestone.md has `| Agent | `unlimited` | `quality` | `balanced` | `budget` |` header with opus for gsd-integration-checker
- debug.md has same header format with opus for gsd-debugger
- execute-phase.md has same header format (indented) with opus for gsd-executor and gsd-verifier
- new-milestone.md has same header format with opus for all 3 agents
  </done>
</task>

<task type="auto">
  <name>Task 2: Add unlimited column to remaining 4 command files</name>
  <files>
    commands/gsd/new-project.md
    commands/gsd/plan-phase.md
    commands/gsd/quick.md
    commands/gsd/research-phase.md
  </files>
  <action>
For each file, update the model lookup table:

**new-project.md (line 391):**
Current:
```markdown
| Agent | quality | balanced | budget |
|-------|---------|----------|--------|
| gsd-project-researcher | opus | sonnet | haiku |
| gsd-research-synthesizer | sonnet | sonnet | haiku |
| gsd-roadmapper | opus | sonnet | sonnet |
```
Target:
```markdown
| Agent | `unlimited` | `quality` | `balanced` | `budget` |
|-------|-------------|-----------|------------|----------|
| gsd-project-researcher | opus | opus | sonnet | haiku |
| gsd-research-synthesizer | opus | sonnet | sonnet | haiku |
| gsd-roadmapper | opus | opus | sonnet | sonnet |
```

**plan-phase.md (line 63):**
Current:
```markdown
| Agent | quality | balanced | budget |
|-------|---------|----------|--------|
| gsd-phase-researcher | opus | sonnet | haiku |
| gsd-planner | opus | opus | sonnet |
| gsd-plan-checker | sonnet | sonnet | haiku |
```
Target:
```markdown
| Agent | `unlimited` | `quality` | `balanced` | `budget` |
|-------|-------------|-----------|------------|----------|
| gsd-phase-researcher | opus | opus | sonnet | haiku |
| gsd-planner | opus | opus | opus | sonnet |
| gsd-plan-checker | opus | sonnet | sonnet | haiku |
```

**quick.md (line 49):**
Current:
```markdown
| Agent | quality | balanced | budget |
|-------|---------|----------|--------|
| gsd-planner | opus | opus | sonnet |
| gsd-executor | opus | sonnet | sonnet |
```
Target:
```markdown
| Agent | `unlimited` | `quality` | `balanced` | `budget` |
|-------|-------------|-----------|------------|----------|
| gsd-planner | opus | opus | opus | sonnet |
| gsd-executor | opus | opus | sonnet | sonnet |
```

**research-phase.md (line 46):**
Current:
```markdown
| Agent | quality | balanced | budget |
|-------|---------|----------|--------|
| gsd-phase-researcher | opus | sonnet | haiku |
```
Target:
```markdown
| Agent | `unlimited` | `quality` | `balanced` | `budget` |
|-------|-------------|-----------|------------|----------|
| gsd-phase-researcher | opus | opus | sonnet | haiku |
```

**Critical format requirements:**
- Use backticks around profile names in header: `unlimited`, `quality`, `balanced`, `budget`
- Add separator column: `-------------|`
- Column order: unlimited first after Agent
- Do NOT modify any other part of the files
  </action>
  <verify>
Run: `grep -A5 "| Agent |" commands/gsd/new-project.md commands/gsd/plan-phase.md commands/gsd/quick.md commands/gsd/research-phase.md`
Confirm each table has 5 columns with unlimited first after Agent.
Confirm all unlimited values are opus.
  </verify>
  <done>
- new-project.md has `| Agent | `unlimited` | `quality` | `balanced` | `budget` |` header with opus for all 3 agents
- plan-phase.md has same header format with opus for all 3 agents
- quick.md has same header format with opus for both agents
- research-phase.md has same header format with opus for gsd-phase-researcher
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. **Column count verification:**
   ```bash
   for f in audit-milestone debug execute-phase new-milestone new-project plan-phase quick research-phase; do
     echo "=== $f.md ==="
     grep "| Agent |.*unlimited.*quality.*balanced.*budget" commands/gsd/$f.md
   done
   ```
   All 8 files must match pattern.

2. **Unlimited value verification:**
   ```bash
   grep -h "opus.*opus\|opus.*sonnet" commands/gsd/{audit-milestone,debug,execute-phase,new-milestone,new-project,plan-phase,quick,research-phase}.md | grep -v "^#"
   ```
   First column after Agent must always be opus.

3. **No regressions in existing columns:**
   ```bash
   git diff --stat commands/gsd/
   ```
   Changes should only be in the 8 target files, and only table modifications.
</verification>

<success_criteria>
- All 8 command files have 5-column model lookup tables
- Column order in all files: Agent | unlimited | quality | balanced | budget
- All unlimited values are opus
- Existing quality/balanced/budget values unchanged
- Header format uses backticks around profile names
- TABLE-02 through TABLE-09 requirements satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/02-command-tables/02-01-SUMMARY.md`
</output>
