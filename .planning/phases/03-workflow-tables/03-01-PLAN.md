---
phase: 03-workflow-tables
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/workflows/execute-phase.md
  - get-shit-done/workflows/execute-plan.md
  - get-shit-done/workflows/map-codebase.md
  - get-shit-done/workflows/verify-work.md
autonomous: true

must_haves:
  truths:
    - "execute-phase.md workflow table includes unlimited column as first profile column"
    - "execute-plan.md workflow table includes unlimited column with opus value"
    - "map-codebase.md workflow table includes unlimited column with opus value"
    - "verify-work.md workflow table includes unlimited column with opus values"
  artifacts:
    - path: "get-shit-done/workflows/execute-phase.md"
      provides: "Model lookup table for phase execution"
      contains: "| Agent | `unlimited` | `quality` | `balanced` | `budget` |"
    - path: "get-shit-done/workflows/execute-plan.md"
      provides: "Model lookup table for plan execution"
      contains: "| Agent | `unlimited` | `quality` | `balanced` | `budget` |"
    - path: "get-shit-done/workflows/map-codebase.md"
      provides: "Model lookup table for codebase mapping"
      contains: "| Agent | `unlimited` | `quality` | `balanced` | `budget` |"
    - path: "get-shit-done/workflows/verify-work.md"
      provides: "Model lookup table for verification"
      contains: "| Agent | `unlimited` | `quality` | `balanced` | `budget` |"
  key_links:
    - from: "all 4 workflow files"
      to: "get-shit-done/references/model-profiles.md"
      via: "consistent column order and opus values"
      pattern: "unlimited.*quality.*balanced.*budget"
---

<objective>
Add the unlimited column to all 4 workflow file lookup tables.

Purpose: Completes the "unlimited" profile rollout to workflow files, enabling users to configure opus for all agents during workflow execution.

Output: 4 updated workflow files with 4-column model lookup tables (unlimited | quality | balanced | budget).
</objective>

<execution_context>
@/Users/vmakaev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vmakaev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-workflow-tables/03-RESEARCH.md

# Reference for target table format and values
@get-shit-done/references/model-profiles.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add unlimited column to all 4 workflow files</name>
  <files>
    get-shit-done/workflows/execute-phase.md
    get-shit-done/workflows/execute-plan.md
    get-shit-done/workflows/map-codebase.md
    get-shit-done/workflows/verify-work.md
  </files>
  <action>
For each of the 4 workflow files, locate the "Model lookup table" section and update the table:

**execute-phase.md** - Update table to:
```markdown
| Agent | `unlimited` | `quality` | `balanced` | `budget` |
|-------|-------------|-----------|------------|----------|
| gsd-executor | opus | opus | sonnet | sonnet |
| gsd-verifier | opus | sonnet | sonnet | haiku |
| general-purpose | --- | --- | --- | --- |
```

**execute-plan.md** - Update table to:
```markdown
| Agent | `unlimited` | `quality` | `balanced` | `budget` |
|-------|-------------|-----------|------------|----------|
| gsd-executor | opus | opus | sonnet | sonnet |
```

**map-codebase.md** - Update table to:
```markdown
| Agent | `unlimited` | `quality` | `balanced` | `budget` |
|-------|-------------|-----------|------------|----------|
| gsd-codebase-mapper | opus | sonnet | haiku | haiku |
```

**verify-work.md** - Update table to:
```markdown
| Agent | `unlimited` | `quality` | `balanced` | `budget` |
|-------|-------------|-----------|------------|----------|
| gsd-planner | opus | opus | opus | sonnet |
| gsd-plan-checker | opus | sonnet | sonnet | haiku |
```

**CRITICAL rules:**
- Column order must be: `unlimited | quality | balanced | budget` (highest to lowest tier)
- Use backticks around profile names in header row
- All unlimited values are `opus` (per decision 01-01)
- general-purpose row keeps dashes (---) for ALL columns including unlimited
- Do NOT modify existing quality/balanced/budget values
  </action>
  <verify>
Run git diff to confirm:
1. Only the 4 workflow files changed
2. Each file has new unlimited column as first profile column
3. Existing quality/balanced/budget values unchanged
4. general-purpose row (if present) has dashes in unlimited column

```bash
git diff get-shit-done/workflows/
```
  </verify>
  <done>
All 4 workflow files have model lookup tables with format:
| Agent | `unlimited` | `quality` | `balanced` | `budget` |
with opus values in unlimited column for all agents (except general-purpose which has ---).
  </done>
</task>

</tasks>

<verification>
1. All 4 workflow files have the unlimited column in their model lookup tables
2. Column order matches: unlimited | quality | balanced | budget
3. All agents have opus in unlimited column (except general-purpose)
4. Existing values in quality/balanced/budget columns are preserved
5. Table formatting is consistent (backticks on headers, proper separator row)

```bash
# Verify all 4 files have the new column structure
grep -A5 "Model lookup table" get-shit-done/workflows/execute-phase.md
grep -A3 "Model lookup table" get-shit-done/workflows/execute-plan.md
grep -A3 "Model lookup table" get-shit-done/workflows/map-codebase.md
grep -A4 "Model lookup table" get-shit-done/workflows/verify-work.md
```
</verification>

<success_criteria>
- [ ] execute-phase.md: Table has 5 columns (Agent + 4 profiles), unlimited column with opus/opus/---
- [ ] execute-plan.md: Table has 5 columns, unlimited column with opus
- [ ] map-codebase.md: Table has 5 columns, unlimited column with opus
- [ ] verify-work.md: Table has 5 columns, unlimited column with opus/opus
- [ ] All tables use backticks on profile names in header
- [ ] git diff shows only unlimited column additions, no other changes
</success_criteria>

<output>
After completion, create `.planning/phases/03-workflow-tables/03-01-SUMMARY.md`
</output>
